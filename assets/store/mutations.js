'use strict';

export default {
    hue(state, hue) {
        state.hue = hue;
    },
    hueMode(state, hueMode) {
        state.hueMode = hueMode;
    },
    hueSpeed(state, hueSpeed) {
        state.hueSpeed = hueSpeed;
    },
    saturation(state, saturation) {
        state.saturation = saturation;
    },
    saturationMode(state, saturationMode) {
        state.saturationMode = saturationMode;
    },
    saturationSpeed(state, saturationSpeed) {
        state.saturationSpeed = saturationSpeed;
    },
    value(state, value) {
        state.value = value;
    },
    valueMode(state, valueMode) {
        state.valueMode = valueMode;
    },
    valueSpeed(state, valueSpeed) {
        state.valueSpeed = valueSpeed;
    }
};
